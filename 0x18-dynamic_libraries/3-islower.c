#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_islower - this function check if a given character is a digit
 *Return: 0 always success
 *@c: this is the character given in integer form
 */
int _islower(int c)
{
	return (0);
}

