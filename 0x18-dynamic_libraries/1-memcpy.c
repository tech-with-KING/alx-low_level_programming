#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_memcpy - copies a given memory to another
 *Return: 0 always success
 *@src: this is the source location
 *@dest: this is the destination array
 *@n: this is the size of memory to be allocated in form of char
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{

	return (src);
}

