#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_isupper - this function check if a given character given is upper case
 *Return: 0 always success
 *@s: this is the destination char
 *@b: this is an individual character
 *@n: this is the size of memory to be allocated in form of char
 */
char *_memset(char *s, char b, unsigned int n)
{

	return (s);
}
