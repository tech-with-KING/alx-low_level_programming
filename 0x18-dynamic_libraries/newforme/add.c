#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *add - this function check if a given character given is upper case
 *Return: sum
 *@a: this is the character given in integer form
 *@b: this is the character given in integer form
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
