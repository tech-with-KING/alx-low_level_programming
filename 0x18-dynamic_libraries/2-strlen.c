#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_strlen - copies a given memory to another
 *Return: 0 always success
 *@s: this is the source location
 */
int _strlen(char *s)
{

	return (0);
}
