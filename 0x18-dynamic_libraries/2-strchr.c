#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_strchr - copies a given memory to another
 *Return: 0 always success
 *@s: this is the source location
 *@c: this is the destination array
 */
char *_strchr(char *s, char c)
{

	return (s);
}
