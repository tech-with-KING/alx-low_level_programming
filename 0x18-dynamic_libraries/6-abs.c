#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_abs - this function check if a given character given is upper case
 *Return: 0 always success
 *@n: this is the character given in integer form
 */
int _abs(int n)
{
	return (0);
}

