#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_isdigit - this function check if a given character is a digit
 *Return: 0 always success
 *@c: this is the character given in integer form
 */
int _isdigit(int c)
{
	return (0);
}
