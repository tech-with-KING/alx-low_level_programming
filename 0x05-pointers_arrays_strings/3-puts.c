#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *_puts - prints a character to the stdio
 *
 *@str: this is going to be the supplied argument
 */
void _puts(char *str)
{
	const char *word;

	strcpy(*str, word);
	puts(word);
}
