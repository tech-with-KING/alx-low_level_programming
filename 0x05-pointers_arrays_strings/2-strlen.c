#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *_strlen - this function gives the length of any string passed to it
 *
 *Return: len the length or character always
 */

int _strlen(char *s)
{
	char len;

	len = *s;
	return (strlen(len));
}
