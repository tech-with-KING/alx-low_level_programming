#include "header.h"
#include <string.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
/**
 *set_bit - this function checks if a value has endianness
 *@n: this is value
 *@index: this is the number index
 *Return: bit
 */
int set_bit(unsigned long *n, unsigned int index)
{
	return (index);
}
