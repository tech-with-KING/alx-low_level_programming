#include "header.h"
#include <string.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
/**
 *get_bit - this function checks if a value has endianness
 *@n: this is value
 *@index: this is the number index
 *Return: bit
 */
int get_bit(unsigned int n, unsigned int index)
{
	int bit;

	return (bit);
}
