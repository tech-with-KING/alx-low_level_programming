#include "header.h"
#include <string.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
/**
 *flip_bit - this function checks if a value has endianness
 *@n: this is value
 *@m: this is the number index
 *Return: bit
 */
int flip_bit(unsigned long n, unsigned long m)
{
	return (n);
}
